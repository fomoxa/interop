// GENERATED BY fomoxac
// DO NOT EDIT MANUALLY
// fingerprint: sha256:b8f0e2543fc9c60d89329c4fa7f31e6f5a0e89600f61f31c1120c5907bfb0b29
// fomoxac-version: 0.2.1
// generated-at: 2026-09-21T09:08:38Z
//
// Every fingerprint this schema publishes, and the handshake that compares
// them. Generated - never edit, and never hand-maintain a copy of these
// values anywhere else.

#![allow(dead_code, unused_imports)]

/// The fingerprint of the whole schema: every message, by name, with its own
/// fingerprint, hashed together. Two peers that agree on this agree on
/// everything.
pub const FOMOXA_SCHEMA_FINGERPRINT: u64 = 0xB8F0E2543FC9C60D;

/// `ChatHeard`, as declared - every annotated field, whatever codec it joined.
pub const CHAT_HEARD_FINGERPRINT: u64 = 0x3E613BAC1DBB8AA9;
/// `ChatHeard.rpc` - the wire contract `ChatHeardRpcCodec` encodes and decodes.
pub const CHAT_HEARD_RPC_MESSAGE_ID: u32 = 0xE037CA19;
pub const CHAT_HEARD_RPC_FINGERPRINT: u64 = 0xE7F7CA08D16B1A00;
/// One fingerprint per prefix of `ChatHeard.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `CHAT_HEARD_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const CHAT_HEARD_RPC_PREFIXES: &[u64] = &[
    0xE7F7CA08D16B1A00,
];

/// `ChatOpen`, as declared - every annotated field, whatever codec it joined.
pub const CHAT_OPEN_FINGERPRINT: u64 = 0xC7D83538D91B7202;
/// `ChatOpen.rpc` - the wire contract `ChatOpenRpcCodec` encodes and decodes.
pub const CHAT_OPEN_RPC_MESSAGE_ID: u32 = 0xEAEB5F14;
pub const CHAT_OPEN_RPC_FINGERPRINT: u64 = 0x3D4AF6B9C83E293B;
/// One fingerprint per prefix of `ChatOpen.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `CHAT_OPEN_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const CHAT_OPEN_RPC_PREFIXES: &[u64] = &[
    0x3D4AF6B9C83E293B,
];

/// `ChatSaid`, as declared - every annotated field, whatever codec it joined.
pub const CHAT_SAID_FINGERPRINT: u64 = 0xA145D739CA2870FA;
/// `ChatSaid.rpc` - the wire contract `ChatSaidRpcCodec` encodes and decodes.
pub const CHAT_SAID_RPC_MESSAGE_ID: u32 = 0xACBEA203;
pub const CHAT_SAID_RPC_FINGERPRINT: u64 = 0xE1399A587A239B43;
/// One fingerprint per prefix of `ChatSaid.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `CHAT_SAID_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const CHAT_SAID_RPC_PREFIXES: &[u64] = &[
    0xE1399A587A239B43,
];

/// `CountItem`, as declared - every annotated field, whatever codec it joined.
pub const COUNT_ITEM_FINGERPRINT: u64 = 0x0A25FCE1CB10E6CC;
/// `CountItem.rpc` - the wire contract `CountItemRpcCodec` encodes and decodes.
pub const COUNT_ITEM_RPC_MESSAGE_ID: u32 = 0x5FB691E2;
pub const COUNT_ITEM_RPC_FINGERPRINT: u64 = 0xA3A551BBFCF90A6C;
/// One fingerprint per prefix of `CountItem.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `COUNT_ITEM_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const COUNT_ITEM_RPC_PREFIXES: &[u64] = &[
    0xA3A551BBFCF90A6C,
];

/// `CountRequest`, as declared - every annotated field, whatever codec it joined.
pub const COUNT_REQUEST_FINGERPRINT: u64 = 0xE6507D7B64D33ADA;
/// `CountRequest.rpc` - the wire contract `CountRequestRpcCodec` encodes and decodes.
pub const COUNT_REQUEST_RPC_MESSAGE_ID: u32 = 0x188823FA;
pub const COUNT_REQUEST_RPC_FINGERPRINT: u64 = 0x12D24DCF758A9769;
/// One fingerprint per prefix of `CountRequest.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `COUNT_REQUEST_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const COUNT_REQUEST_RPC_PREFIXES: &[u64] = &[
    0x12D24DCF758A9769,
];

/// `EchoRequest`, as declared - every annotated field, whatever codec it joined.
pub const ECHO_REQUEST_FINGERPRINT: u64 = 0x867E1703F1C448E8;
/// `EchoRequest.rpc` - the wire contract `EchoRequestRpcCodec` encodes and decodes.
pub const ECHO_REQUEST_RPC_MESSAGE_ID: u32 = 0x109F8C60;
pub const ECHO_REQUEST_RPC_FINGERPRINT: u64 = 0xD477F0362150CB3D;
/// One fingerprint per prefix of `EchoRequest.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `ECHO_REQUEST_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const ECHO_REQUEST_RPC_PREFIXES: &[u64] = &[
    0xD477F0362150CB3D,
];

/// `EchoResponse`, as declared - every annotated field, whatever codec it joined.
pub const ECHO_RESPONSE_FINGERPRINT: u64 = 0x53AE11C4D04C68E1;
/// `EchoResponse.rpc` - the wire contract `EchoResponseRpcCodec` encodes and decodes.
pub const ECHO_RESPONSE_RPC_MESSAGE_ID: u32 = 0x96D79DED;
pub const ECHO_RESPONSE_RPC_FINGERPRINT: u64 = 0x23368E8A5E0FCA18;
/// One fingerprint per prefix of `EchoResponse.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `ECHO_RESPONSE_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const ECHO_RESPONSE_RPC_PREFIXES: &[u64] = &[
    0x23368E8A5E0FCA18,
];

/// `FRpcCredit`, as declared - every annotated field, whatever codec it joined.
pub const FRPC_CREDIT_FINGERPRINT: u64 = 0x4FA49BC6A748D5DF;
/// `FRpcCredit.rpc` - the wire contract `FRpcCreditRpcCodec` encodes and decodes.
pub const FRPC_CREDIT_RPC_MESSAGE_ID: u32 = 0x789A010F;
pub const FRPC_CREDIT_RPC_FINGERPRINT: u64 = 0x9F3D9AEE476F42BD;
/// One fingerprint per prefix of `FRpcCredit.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `FRPC_CREDIT_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const FRPC_CREDIT_RPC_PREFIXES: &[u64] = &[
    0x9F3D9AEE476F42BD,
];

/// `FRpcCtx`, as declared - every annotated field, whatever codec it joined.
pub const FRPC_CTX_FINGERPRINT: u64 = 0x95279604B295AF5F;
/// `FRpcCtx.rpc` - the wire contract `FRpcCtxRpcCodec` encodes and decodes.
pub const FRPC_CTX_RPC_MESSAGE_ID: u32 = 0x5AE3B9AC;
pub const FRPC_CTX_RPC_FINGERPRINT: u64 = 0x3B43F47205E0362D;
/// One fingerprint per prefix of `FRpcCtx.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `FRPC_CTX_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const FRPC_CTX_RPC_PREFIXES: &[u64] = &[
    0x8001ED8EC14681BB,
    0x0E639D9A7BA779AE,
    0x30DC5256C33F9BC2,
    0x2B280600965F971E,
    0x98AB443D256795B2,
    0xA35FE2811247558B,
    0x3B43F47205E0362D,
];

/// `FRpcError`, as declared - every annotated field, whatever codec it joined.
pub const FRPC_ERROR_FINGERPRINT: u64 = 0x2D983E2470F35222;
/// `FRpcError.rpc` - the wire contract `FRpcErrorRpcCodec` encodes and decodes.
pub const FRPC_ERROR_RPC_MESSAGE_ID: u32 = 0xBF94ECC4;
pub const FRPC_ERROR_RPC_FINGERPRINT: u64 = 0x4A043B62FDC0E07D;
/// One fingerprint per prefix of `FRpcError.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `FRPC_ERROR_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const FRPC_ERROR_RPC_PREFIXES: &[u64] = &[
    0xA58EA55F6A679084,
    0x4A043B62FDC0E07D,
];

/// `FRpcMethodInfo`, as declared - every annotated field, whatever codec it joined.
pub const FRPC_METHOD_INFO_FINGERPRINT: u64 = 0x0E3DDDCAFC83D424;
/// `FRpcMethodInfo.rpc` - the wire contract `FRpcMethodInfoRpcCodec` encodes and decodes.
pub const FRPC_METHOD_INFO_RPC_MESSAGE_ID: u32 = 0x6D10D388;
pub const FRPC_METHOD_INFO_RPC_FINGERPRINT: u64 = 0xF0B7D74B8A72541B;
/// One fingerprint per prefix of `FRpcMethodInfo.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `FRPC_METHOD_INFO_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const FRPC_METHOD_INFO_RPC_PREFIXES: &[u64] = &[
    0x1CFCAACFF8F74D16,
    0x40160EB735B6818A,
    0x9C5C415004A2F8C1,
    0x577AD38C2371DC87,
    0x76D63BA5A1D8AA4D,
    0xF0B7D74B8A72541B,
];

/// `FRpcReflectRequest`, as declared - every annotated field, whatever codec it joined.
pub const FRPC_REFLECT_REQUEST_FINGERPRINT: u64 = 0x951C826E7EA8DB69;
/// `FRpcReflectRequest.rpc` - the wire contract `FRpcReflectRequestRpcCodec` encodes and decodes.
pub const FRPC_REFLECT_REQUEST_RPC_MESSAGE_ID: u32 = 0x9C0C80F3;
pub const FRPC_REFLECT_REQUEST_RPC_FINGERPRINT: u64 = 0xAAAB56B8165C245B;
/// One fingerprint per prefix of `FRpcReflectRequest.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `FRPC_REFLECT_REQUEST_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const FRPC_REFLECT_REQUEST_RPC_PREFIXES: &[u64] = &[
];

/// `FRpcReflectResponse`, as declared - every annotated field, whatever codec it joined.
pub const FRPC_REFLECT_RESPONSE_FINGERPRINT: u64 = 0x15F4E8E3FCF0935D;
/// `FRpcReflectResponse.rpc` - the wire contract `FRpcReflectResponseRpcCodec` encodes and decodes.
pub const FRPC_REFLECT_RESPONSE_RPC_MESSAGE_ID: u32 = 0xD42A8CD7;
pub const FRPC_REFLECT_RESPONSE_RPC_FINGERPRINT: u64 = 0x2A2C539FD24D8B44;
/// One fingerprint per prefix of `FRpcReflectResponse.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `FRPC_REFLECT_RESPONSE_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const FRPC_REFLECT_RESPONSE_RPC_PREFIXES: &[u64] = &[
    0xFD550335C35A454B,
    0x2A2C539FD24D8B44,
];

/// `FRpcVoid`, as declared - every annotated field, whatever codec it joined.
pub const FRPC_VOID_FINGERPRINT: u64 = 0xCD48EE6983D59C6F;
/// `FRpcVoid.rpc` - the wire contract `FRpcVoidRpcCodec` encodes and decodes.
pub const FRPC_VOID_RPC_MESSAGE_ID: u32 = 0xD4A891C4;
pub const FRPC_VOID_RPC_FINGERPRINT: u64 = 0x2B58FE10F955C833;
/// One fingerprint per prefix of `FRpcVoid.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `FRPC_VOID_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const FRPC_VOID_RPC_PREFIXES: &[u64] = &[
];

/// `TicksItem`, as declared - every annotated field, whatever codec it joined.
pub const TICKS_ITEM_FINGERPRINT: u64 = 0x36CA22D0E95BB4A3;
/// `TicksItem.rpc` - the wire contract `TicksItemRpcCodec` encodes and decodes.
pub const TICKS_ITEM_RPC_MESSAGE_ID: u32 = 0x35BE4510;
pub const TICKS_ITEM_RPC_FINGERPRINT: u64 = 0x820BA1C0FDACDCFC;
/// One fingerprint per prefix of `TicksItem.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `TICKS_ITEM_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const TICKS_ITEM_RPC_PREFIXES: &[u64] = &[
    0x820BA1C0FDACDCFC,
];

/// `TicksRequest`, as declared - every annotated field, whatever codec it joined.
pub const TICKS_REQUEST_FINGERPRINT: u64 = 0xBC709A0BDB78D258;
/// `TicksRequest.rpc` - the wire contract `TicksRequestRpcCodec` encodes and decodes.
pub const TICKS_REQUEST_RPC_MESSAGE_ID: u32 = 0xE96A59D5;
pub const TICKS_REQUEST_RPC_FINGERPRINT: u64 = 0x68C7B9B2E11A98D3;
/// One fingerprint per prefix of `TicksRequest.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `TICKS_REQUEST_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const TICKS_REQUEST_RPC_PREFIXES: &[u64] = &[
];

/// `UploadChunk`, as declared - every annotated field, whatever codec it joined.
pub const UPLOAD_CHUNK_FINGERPRINT: u64 = 0x42F9F0643664047B;
/// `UploadChunk.rpc` - the wire contract `UploadChunkRpcCodec` encodes and decodes.
pub const UPLOAD_CHUNK_RPC_MESSAGE_ID: u32 = 0x65DB5549;
pub const UPLOAD_CHUNK_RPC_FINGERPRINT: u64 = 0xB28344E68B53BAB6;
/// One fingerprint per prefix of `UploadChunk.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `UPLOAD_CHUNK_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const UPLOAD_CHUNK_RPC_PREFIXES: &[u64] = &[
    0xB28344E68B53BAB6,
];

/// `UploadDone`, as declared - every annotated field, whatever codec it joined.
pub const UPLOAD_DONE_FINGERPRINT: u64 = 0xBF23C2244FA92353;
/// `UploadDone.rpc` - the wire contract `UploadDoneRpcCodec` encodes and decodes.
pub const UPLOAD_DONE_RPC_MESSAGE_ID: u32 = 0x0386DCA3;
pub const UPLOAD_DONE_RPC_FINGERPRINT: u64 = 0x7A9CB5B5480D918A;
/// One fingerprint per prefix of `UploadDone.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `UPLOAD_DONE_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const UPLOAD_DONE_RPC_PREFIXES: &[u64] = &[
    0x7A9CB5B5480D918A,
];

/// `UploadOpen`, as declared - every annotated field, whatever codec it joined.
pub const UPLOAD_OPEN_FINGERPRINT: u64 = 0xEDADF76EC1636AD0;
/// `UploadOpen.rpc` - the wire contract `UploadOpenRpcCodec` encodes and decodes.
pub const UPLOAD_OPEN_RPC_MESSAGE_ID: u32 = 0x315285E4;
pub const UPLOAD_OPEN_RPC_FINGERPRINT: u64 = 0x46A5DF3127C6E384;
/// One fingerprint per prefix of `UploadOpen.rpc`: entry `k-1` covers its first `k`
/// fields. The last entry is `UPLOAD_OPEN_RPC_FINGERPRINT`. Never sent whole - a peer
/// sends its field count and its last entry, and the two sides compare at
/// `min` of the two counts (RFC-0002 §9.1).
pub const UPLOAD_OPEN_RPC_PREFIXES: &[u64] = &[
    0x46A5DF3127C6E384,
];

/// One message: its id, its name, and the fingerprint of its wire contract.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[allow(dead_code)]
pub struct FomoxaMessage {
    /// Stable across schema changes; derived from the name alone.
    pub id: u32,
    /// `Model.codec`.
    pub name: &'static str,
    /// Changes whenever the message's fields do.
    pub fingerprint: u64,
    /// One entry per field: entry `k-1` covers the first `k` fields. The last
    /// entry is `fingerprint`. Stays local; only its length and its last entry
    /// ever go on the wire.
    pub prefixes: &'static [u64],
}

/// Every message this schema declares, sorted by id.
#[allow(dead_code)]
pub const FOMOXA_MESSAGES: &[FomoxaMessage] = &[
    FomoxaMessage { id: UPLOAD_DONE_RPC_MESSAGE_ID, name: "UploadDone.rpc", fingerprint: UPLOAD_DONE_RPC_FINGERPRINT, prefixes: UPLOAD_DONE_RPC_PREFIXES },
    FomoxaMessage { id: ECHO_REQUEST_RPC_MESSAGE_ID, name: "EchoRequest.rpc", fingerprint: ECHO_REQUEST_RPC_FINGERPRINT, prefixes: ECHO_REQUEST_RPC_PREFIXES },
    FomoxaMessage { id: COUNT_REQUEST_RPC_MESSAGE_ID, name: "CountRequest.rpc", fingerprint: COUNT_REQUEST_RPC_FINGERPRINT, prefixes: COUNT_REQUEST_RPC_PREFIXES },
    FomoxaMessage { id: UPLOAD_OPEN_RPC_MESSAGE_ID, name: "UploadOpen.rpc", fingerprint: UPLOAD_OPEN_RPC_FINGERPRINT, prefixes: UPLOAD_OPEN_RPC_PREFIXES },
    FomoxaMessage { id: TICKS_ITEM_RPC_MESSAGE_ID, name: "TicksItem.rpc", fingerprint: TICKS_ITEM_RPC_FINGERPRINT, prefixes: TICKS_ITEM_RPC_PREFIXES },
    FomoxaMessage { id: FRPC_CTX_RPC_MESSAGE_ID, name: "FRpcCtx.rpc", fingerprint: FRPC_CTX_RPC_FINGERPRINT, prefixes: FRPC_CTX_RPC_PREFIXES },
    FomoxaMessage { id: COUNT_ITEM_RPC_MESSAGE_ID, name: "CountItem.rpc", fingerprint: COUNT_ITEM_RPC_FINGERPRINT, prefixes: COUNT_ITEM_RPC_PREFIXES },
    FomoxaMessage { id: UPLOAD_CHUNK_RPC_MESSAGE_ID, name: "UploadChunk.rpc", fingerprint: UPLOAD_CHUNK_RPC_FINGERPRINT, prefixes: UPLOAD_CHUNK_RPC_PREFIXES },
    FomoxaMessage { id: FRPC_METHOD_INFO_RPC_MESSAGE_ID, name: "FRpcMethodInfo.rpc", fingerprint: FRPC_METHOD_INFO_RPC_FINGERPRINT, prefixes: FRPC_METHOD_INFO_RPC_PREFIXES },
    FomoxaMessage { id: FRPC_CREDIT_RPC_MESSAGE_ID, name: "FRpcCredit.rpc", fingerprint: FRPC_CREDIT_RPC_FINGERPRINT, prefixes: FRPC_CREDIT_RPC_PREFIXES },
    FomoxaMessage { id: ECHO_RESPONSE_RPC_MESSAGE_ID, name: "EchoResponse.rpc", fingerprint: ECHO_RESPONSE_RPC_FINGERPRINT, prefixes: ECHO_RESPONSE_RPC_PREFIXES },
    FomoxaMessage { id: FRPC_REFLECT_REQUEST_RPC_MESSAGE_ID, name: "FRpcReflectRequest.rpc", fingerprint: FRPC_REFLECT_REQUEST_RPC_FINGERPRINT, prefixes: FRPC_REFLECT_REQUEST_RPC_PREFIXES },
    FomoxaMessage { id: CHAT_SAID_RPC_MESSAGE_ID, name: "ChatSaid.rpc", fingerprint: CHAT_SAID_RPC_FINGERPRINT, prefixes: CHAT_SAID_RPC_PREFIXES },
    FomoxaMessage { id: FRPC_ERROR_RPC_MESSAGE_ID, name: "FRpcError.rpc", fingerprint: FRPC_ERROR_RPC_FINGERPRINT, prefixes: FRPC_ERROR_RPC_PREFIXES },
    FomoxaMessage { id: FRPC_REFLECT_RESPONSE_RPC_MESSAGE_ID, name: "FRpcReflectResponse.rpc", fingerprint: FRPC_REFLECT_RESPONSE_RPC_FINGERPRINT, prefixes: FRPC_REFLECT_RESPONSE_RPC_PREFIXES },
    FomoxaMessage { id: FRPC_VOID_RPC_MESSAGE_ID, name: "FRpcVoid.rpc", fingerprint: FRPC_VOID_RPC_FINGERPRINT, prefixes: FRPC_VOID_RPC_PREFIXES },
    FomoxaMessage { id: CHAT_HEARD_RPC_MESSAGE_ID, name: "ChatHeard.rpc", fingerprint: CHAT_HEARD_RPC_FINGERPRINT, prefixes: CHAT_HEARD_RPC_PREFIXES },
    FomoxaMessage { id: TICKS_REQUEST_RPC_MESSAGE_ID, name: "TicksRequest.rpc", fingerprint: TICKS_REQUEST_RPC_FINGERPRINT, prefixes: TICKS_REQUEST_RPC_PREFIXES },
    FomoxaMessage { id: CHAT_OPEN_RPC_MESSAGE_ID, name: "ChatOpen.rpc", fingerprint: CHAT_OPEN_RPC_FINGERPRINT, prefixes: CHAT_OPEN_RPC_PREFIXES },
];

/// What a peer's fingerprints mean for this one.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[allow(dead_code)]
pub enum FomoxaHandshake {
    /// The same schema, exactly.
    Current,
    /// A different schema, but every message both ends know agrees on the
    /// fields both ends carry. Safe to proceed.
    Outdated,
    /// Both ends put different fields at an index both of them carry. There is
    /// nothing to negotiate: disconnect.
    Reject,
    /// Not decidable from the peer's table alone - at least one message needs
    /// the extra exchange described on `FomoxaMessageCheck::NeedPrefix`.
    NeedMore,
}

/// The message with this id, if this schema declares it.
#[allow(dead_code)]
pub fn fomoxa_message(id: u32) -> ::core::option::Option<&'static FomoxaMessage> {
    let mut low = 0usize;
    let mut high = FOMOXA_MESSAGES.len();
    while low < high {
        let middle = (low + high) / 2;
        if FOMOXA_MESSAGES[middle].id < id {
            low = middle + 1;
        } else {
            high = middle;
        }
    }
    match FOMOXA_MESSAGES.get(low) {
        ::core::option::Option::Some(message) if message.id == id => {
            ::core::option::Option::Some(message)
        }
        _ => ::core::option::Option::None,
    }
}

/// This schema's fingerprint for the first `field_count` fields of a message,
/// or `None` if it does not declare that message or does not have that many
/// fields. `field_count` counts from 1; 0 is the empty prefix and has no
/// fingerprint because it always matches.
#[allow(dead_code)]
pub fn fomoxa_prefix(id: u32, field_count: u32) -> ::core::option::Option<u64> {
    let message = fomoxa_message(id)?;
    if field_count == 0 {
        return ::core::option::Option::None;
    }
    message.prefixes.get(field_count as usize - 1).copied()
}

/// What one of the peer's messages means for this schema's message of the same
/// id. See [`fomoxa_message_check`].
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[allow(dead_code)]
pub enum FomoxaMessageCheck {
    /// Either this schema does not declare the message at all, or the fields
    /// both ends carry agree. Nothing to do.
    Match,
    /// Both ends put different fields at an index both of them carry.
    Reject,
    /// Undecidable from what the peer sent: the peer has more fields than this
    /// schema, so the answer lives at an index only the peer can produce. Ask
    /// it for its prefix fingerprint at this field count, then feed the reply
    /// to `fomoxa_prefix` for the same id and compare.
    NeedPrefix(u32),
}

/// Compares one of the peer's messages against this schema's.
///
/// `peer_field_count` and `peer_fingerprint` are what the peer declares for
/// this id - its field count and the fingerprint of its whole message. This is
/// RFC-0002 §9.1's prefix test: the two are compatible when the shorter field
/// list is an exact prefix of the longer one, so the comparison happens at
/// `min(peer_field_count, local field count)`.
#[allow(dead_code)]
pub fn fomoxa_message_check(
    id: u32,
    peer_field_count: u32,
    peer_fingerprint: u64,
) -> FomoxaMessageCheck {
    let ::core::option::Option::Some(known) = fomoxa_message(id) else {
        // Not a message this schema declares, so it is never exchanged.
        return FomoxaMessageCheck::Match;
    };
    let local_field_count = known.prefixes.len() as u32;

    if peer_fingerprint == known.fingerprint {
        return FomoxaMessageCheck::Match;
    }
    if peer_field_count == 0 || local_field_count == 0 {
        // The empty field list is a prefix of everything.
        return FomoxaMessageCheck::Match;
    }
    if peer_field_count == local_field_count {
        // Same length, different content - a prefix of equal length would have
        // to be equality, and it is not.
        return FomoxaMessageCheck::Reject;
    }
    if peer_field_count < local_field_count {
        // The peer's own fingerprint already is the value at the shared index.
        return match known.prefixes.get(peer_field_count as usize - 1) {
            ::core::option::Option::Some(local) if *local == peer_fingerprint => {
                FomoxaMessageCheck::Match
            }
            _ => FomoxaMessageCheck::Reject,
        };
    }
    FomoxaMessageCheck::NeedPrefix(local_field_count)
}

/// Compares a peer's whole message table against this schema's.
///
/// `peer_messages` is the peer's `(id, field count, fingerprint)` table - what
/// `FOMOXA_MESSAGES` is on its side. A `NeedMore` result means at least one
/// message needs the extra round described on
/// [`FomoxaMessageCheck::NeedPrefix`]; walk the table with
/// `fomoxa_message_check` to find which ones.
#[allow(dead_code)]
pub fn fomoxa_handshake(
    peer_schema_fingerprint: u64,
    peer_messages: &[(u32, u32, u64)],
) -> FomoxaHandshake {
    if peer_schema_fingerprint == FOMOXA_SCHEMA_FINGERPRINT {
        return FomoxaHandshake::Current;
    }

    let mut need_more = false;
    for (id, field_count, fingerprint) in peer_messages {
        match fomoxa_message_check(*id, *field_count, *fingerprint) {
            // One mismatch decides the whole session. Every other message
            // could agree and it would still be unsafe to speak.
            FomoxaMessageCheck::Reject => return FomoxaHandshake::Reject,
            FomoxaMessageCheck::NeedPrefix(_) => need_more = true,
            FomoxaMessageCheck::Match => {}
        }
    }

    if need_more {
        return FomoxaHandshake::NeedMore;
    }
    FomoxaHandshake::Outdated
}

/// Whether this schema was generated with `validate_message_fingerprint`.
#[allow(dead_code)]
pub const FOMOXA_VALIDATE_MESSAGE_FINGERPRINT: bool = false;
// Per-frame message validation is off, so no envelope is generated and no
// frame carries one. Turn it on in fomoxa.toml:
//
//     validate_message_fingerprint = true
//
// and every frame gains [MessageId: u32][MessageFingerprint: u64] in front of
// its payload, with fomoxa_write_envelope / fomoxa_read_envelope to match.
