# The demo project the examples are built from, laid out the way a real crate is:
# annotated models under src/models/, and the tree fomoxac writes beside them at
# src/generated/. Regenerate from this directory:
#
#     fomoxac generate
src = "src"
out = "src/generated"
